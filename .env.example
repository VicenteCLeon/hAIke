# .env.local — copia este archivo
# NUNCA subas .env.local a git

# Google Gemini — obtén tu key GRATIS en https://aistudio.google.com
# Click en "Get API Key" → "Create API key"
GEMINI_API_KEY=AIza...

# Mapbox — opcional para el mapa (gratis en https://account.mapbox.com)
NEXT_PUBLIC_MAPBOX_TOKEN=pk.eyJ1...

# Open-Meteo, Overpass y Nominatim no requieren API key
